import { CustomWidgetDirective } from './custom-widget.directive';

describe('CustomWidgetDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomWidgetDirective();
    expect(directive).toBeTruthy();
  });
});
